import classNames from 'classnames';
import React from 'react'

function Main({ className }:any) {
  return (
    <div className={classNames([className, 'flex flex-col'])}>

    </div>
  )
}

export default Main;